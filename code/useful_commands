# Find and remove slurm .out files
find . -name "*.out" -type f -delete

# Find and remove slurm .out files
find . -name "*.err" -type f -delete

# Find and remove jupyter .log files
find . -name "*.log" -type f -delete

# Find and remove mcmc reports
find  . -name "iter_*" -type f -delete

# BE CAREFUL WITH NEXT COMMAND
# Remove data/ outputs
rm data/*

# Copy files from MSI to local device
scp neher015@mesabi.msi.umn.edu:abcd_multiview/data/2023-07-31_image_1.RData 2023-07-31_image_1.RData

# Copy a single file from local to remote with scp
scp myfile.txt remoteuser@remoteserver:/remote/folder/
scp code/sample_gamma_eta.R neher015@mesabi.msi.umn.edu:abcd_multiview/code/sample_gamma_eta.R

# Refresh SSH connection fingerprints if they've recently changed
ssh-keygen -R <host>
# For the example,
ssh-keygen -R mesabi.msi.umn.edu.

